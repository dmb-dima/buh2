
////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ УПРАВЛЕНИЯ КОМАНДНОЙ ПАНЕЛИ
                      

Процедура КнопкаВвестиДокументНажатие(Элемент)

	Если ЭлементыФормы.Список.ТекущаяСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ФормаНового = Документы.ОперацияБух.ПолучитьФормуНовогоДокумента();
	ФормаНового.Заполнить(ЭлементыФормы.Список.ТекущиеДанные.Ссылка);

	ФормаНового.Открыть();

КонецПроцедуры

Процедура ИсторияВыбораТиповыхОперацийНажатие(Кнопка)

	Форма = Справочники.ТиповыеОперации.ПолучитьФорму("ИсторияВыбора");
	ТО    = Форма.ОткрытьМодально();

	Если ТО = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ФормаНового = Документы.ОперацияБух.ПолучитьФормуНовогоДокумента();
	ФормаНового.Заполнить(ТО);
	ФормаНового.Открыть();

КонецПроцедуры

Процедура Обмен(Кнопка)

	ФормаОбмена = Справочники.ТиповыеОперации.ПолучитьФорму("ФормаЭкспортаИмпорта", ЭтаФорма);
	ФормаОбмена.Открыть();

КонецПроцедуры




