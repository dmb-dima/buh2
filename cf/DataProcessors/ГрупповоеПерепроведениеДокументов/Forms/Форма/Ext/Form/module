Процедура КнопкаВыполнитьНажатие(Кнопка)
	Если ВыборОрганизации = 0 Тогда
		ПерепроведениеДокументов(ДатаНач, ДатаКон);
	Иначе
		Если СписокОрганизаций.Количество() > 0 Тогда
			ПерепроведениеДокументов(ДатаНач, ДатаКон, СписокОрганизаций.ВыгрузитьКолонку("Организация"));
		КонецЕсли;
	КонецЕсли;	
КонецПроцедуры

Процедура ПоВсемОрганизациямПриИзменении(Элемент)
	ДоступностьВыбораОрганизаций =  (ВыборОрганизации = 1);
	ЭлементыФормы.КоманднаяПанельСписокОрганизация.Доступность = ДоступностьВыбораОрганизаций;
	ЭлементыФормы.СписокОрганизаций.Доступность = ДоступностьВыбораОрганизаций;
	
	Если НЕ ДоступностьВыбораОрганизаций Тогда
		СписокОрганизаций.Очистить();
	КонецЕсли;
	
КонецПроцедуры

Процедура ДатаНачПриИзменении(Элемент)
	
	ЭлементыФормы.ДатаКон.СписокВыбора = СформироватьСписокДатОкончанияПроведения(ДатаНач);
	
	ЕстьВыборДатыКон = (ЭлементыФормы.ДатаКон.СписокВыбора.Количество() > 1);
	
	ЭлементыФормы.НадписьДатаКон.Видимость = ЕстьВыборДатыКон;
	ЭлементыФормы.ДатаКон.Видимость        = ЕстьВыборДатыКон;
	
	ДатаКон = ЭлементыФормы.ДатаКон.СписокВыбора[0].Значение;
	
КонецПроцедуры

Процедура ДатаКонНачалоВыбора(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
КонецПроцедуры
