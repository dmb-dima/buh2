
Процедура ПередЗакрытием(Отказ, СтандартнаяОбработка)
	
	КаталогВремФайлов = КаталогВременныхФайлов();
	КаталогВремФайлов = ?(Прав(КаталогВремФайлов, 1) = "\", КаталогВремФайлов, КаталогВремФайлов + "\");
	ИмяФайлаСправки  = КаталогВремФайлов + "SetupPlatform\";
	
	ФайлПроверки = Новый Файл(ИмяФайлаСправки);
	Если ФайлПроверки.Существует() Тогда
		УдалитьФайлы(ИмяФайлаСправки, "*");
	КонецЕсли; 
	
	ИмяФайлаАрхива  = КаталогВремФайлов + "SetupPlatform.zip";
	ФайлПроверки = Новый Файл(ИмяФайлаАрхива);
	Если ФайлПроверки.Существует() Тогда
		УдалитьФайлы(ИмяФайлаАрхива);
	КонецЕсли; 
	
КонецПроцедуры
