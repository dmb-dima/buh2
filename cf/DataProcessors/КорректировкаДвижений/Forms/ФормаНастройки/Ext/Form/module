
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Для Каждого Строка Из Регистры Цикл
		СтрокаСписка = СписокРегистров.НайтиПоЗначению(Строка.Имя);
		Если НЕ СтрокаСписка = Неопределено Тогда
			Строка.Отображение = СтрокаСписка.Пометка;
		КонецЕсли;
	КонецЦикла;
	Закрыть();
КонецПроцедуры

Процедура ПриОткрытии()
	Регистры.Сортировать("Синоним");
	Для Каждого Строка Из Регистры Цикл
		СписокРегистров.Добавить(Строка.Имя, Строка.Синоним, Строка.Отображение); 
	КонецЦикла
КонецПроцедуры

Процедура КоманднаяПанельСпискаУстановитьФлажки(Кнопка)
	Для Каждого Элемент Из СписокРегистров Цикл
		Элемент.Пометка = Истина;
	КонецЦикла	
КонецПроцедуры

Процедура КоманднаяПанельСпискаСнятьФлажки(Кнопка)
	Для Каждого Элемент Из СписокРегистров Цикл
		Элемент.Пометка = Ложь;
	КонецЦикла	
КонецПроцедуры

Процедура КоманднаяПанельСпискаСтандартнаяНастройка(Кнопка)
	Для Каждого Строка Из Регистры Цикл
		СтрокаСписка = СписокРегистров.НайтиПоЗначению(Строка.Имя);
		Если НЕ СтрокаСписка = Неопределено Тогда
			СтрокаСписка.Пометка = Строка.ЕстьДвижения;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры
