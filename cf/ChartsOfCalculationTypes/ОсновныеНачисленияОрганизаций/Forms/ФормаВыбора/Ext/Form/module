Процедура ПерехватчикСобытия(Элемент)
	ФормаВидаРасчета = ПланыВидовРасчета.ОсновныеНачисленияОрганизаций.ПолучитьФорму("ФормаВидаРасчета", ЭтаФорма);
	ФормаВидаРасчета.Открыть();
	
КонецПроцедуры

Процедура ПланВидовРасчетаСписокПередНачаломДобавления(Элемент, Отказ, Копирование)
	ПерехватчикСобытия(Элемент);
	Отказ = Истина;
	
КонецПроцедуры




























