
Процедура ОсновныеДействияФормыДействие(Кнопка)
	
	Закрыть();
	
КонецПроцедуры

Процедура ДвиженияДокументовПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ДанныеСтроки.Проведен = Неопределено Тогда
	
		ОформлениеСтроки.Ячейки.Объект.Картинка = БиблиотекаКартинок.История;
		
	ИначеЕсли ДанныеСтроки.Проведен = Истина ТОгда
		
		ОформлениеСтроки.Ячейки.Объект.Картинка = БиблиотекаКартинок.Утвердить;
		
	Иначе
		
		ОформлениеСтроки.Ячейки.Объект.Картинка = БиблиотекаКартинок.Отклонить;
		
	КонецЕсли;
			
	ОформлениеСтроки.Ячейки.Объект.ОтображатьКартинку = Истина;
	
КонецПроцедуры
