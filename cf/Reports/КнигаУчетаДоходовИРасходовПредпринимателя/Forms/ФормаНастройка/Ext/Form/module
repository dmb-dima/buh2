Перем Флажки;

Процедура КнопкаОКНажатие(Элемент)
	
	ВсеФлажкиСброшены = Истина;
	Для Каждого Таблица Из СписокТаблиц Цикл
		Если Таблица.Формировать Тогда
			ВсеФлажкиСброшены = Ложь;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ВсеФлажкиСброшены Тогда
		СписокТаблиц[0].Формировать = Истина;
	КонецЕсли;
	
	Закрыть(Истина);
	
КонецПроцедуры

Процедура КнопкаЗакрытьНажатие(Элемент)
	
	СписокТаблиц.ЗагрузитьКолонку(Флажки, "Формировать");
	Закрыть(Истина);
	
КонецПроцедуры

Процедура ПереключитьФлажки(Кнопка)
	
	Если Кнопка.Имя = "Включить" Тогда
		Значение = Истина;
		
	ИначеЕсли Кнопка.Имя = "Выключить" Тогда
		Значение = Ложь;
		
	Иначе
		Возврат;
	КонецЕсли;
	
	Для Каждого Элемент Из СписокТаблиц Цикл
		Элемент.Формировать = Значение;
	КонецЦикла;
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	Флажки = СписокТаблиц.ВыгрузитьКолонку("Формировать");
	
КонецПроцедуры
