////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

// Обработчик события открытия формы
Процедура ПриОткрытии()
	
	// Доступные способы отражения в учете ЕСН (те способы, по которым может быть указан размер скидки)
	СписокДоступныхКодовЕСН = Новый СписокЗначений;
	СписокДоступныхКодовЕСН.Добавить(Справочники.ДоходыЕСН.Матпомощь);
	ЭлементыФормы.РегистрСведенийСписок.Колонки.КодДоходаЕСН.ЭлементУправления.СписокВыбора = СписокДоступныхКодовЕСН;
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ТАБЛИЧНОГО ПОЛЯ "РегистрСведенийСписок"

Процедура РегистрСведенийСписокПриНачалеРедактирования(Элемент, НоваяСтрока)
	Если НоваяСтрока Тогда
		ВведеннаяСтрока = Элемент.ТекущиеДанные;
        Если НЕ ЗначениеЗаполнено(ВведеннаяСтрока.КодДоходаЕСН) Тогда
			 ВведеннаяСтрока.КодДоходаЕСН = Справочники.ДоходыЕСН.Матпомощь
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Процедура РегистрСведенийСписокКодДоходаЕСННачалоВыбора(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь
КонецПроцедуры

