
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Если КонтекстЭДО <> Неопределено Тогда
		КонтекстЭДО.ПередОткрытиемФормы("РегистрСведений", "ТранспортныеКонтейнеры", "ФормаСписка", ЭтаФорма, Отказ, СтандартнаяОбработка);
	Иначе
		Отказ = Истина;
		Предупреждение("Показ формы невозможен: модуль документооборота недоступен!");
	КонецЕсли;
	
КонецПроцедуры
