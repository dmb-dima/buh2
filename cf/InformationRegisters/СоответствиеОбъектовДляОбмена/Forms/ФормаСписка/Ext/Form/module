
Процедура РегистрСведенийСписокПередУдалением(Элемент, Отказ)
	
	Если Элемент.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	НаборЗаписей = РегистрыСведений.СоответствиеОбъектовДляОбмена.СоздатьНаборЗаписей();
	
	НаборЗаписей.Отбор.УзелОбмена.Установить(Элемент.ТекущиеДанные.УзелОбмена);
	НаборЗаписей.Отбор.СсылкаВДругойИБ.Установить(Элемент.ТекущиеДанные.СсылкаВДругойИБ);
	НаборЗаписей.Отбор.СобственнаяСсылка.Установить(Элемент.ТекущиеДанные.СобственнаяСсылка);
	
	НаборЗаписей.Прочитать();
		
	Попытка
		ПланыОбмена.ЗарегистрироватьИзменения(НаборЗаписей.Отбор.УзелОбмена.Значение, НаборЗаписей);
	Исключение
	КонецПопытки;
		
КонецПроцедуры

